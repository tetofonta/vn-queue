[General]

[MM]
network = MM1FIFO
repeat=4
*.gen.scheduleInterval = exponential(1)
*.srv.queue_depth = ${QL=1,3,10,100}
*.srv.scheduleInterval = exponential(${R=0.2,0.5,0.8,1.3})

[MN]
network = MM1FIFO
repeat=4
*.gen.scheduleInterval = exponential(1)
*.srv.queue_depth = ${QL=1,3,10,100}
*.srv.scheduleInterval = truncnormal(${R=0.2,0.5,0.8,1.3}, 1)